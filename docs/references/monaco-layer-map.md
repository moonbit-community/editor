# Monaco Layer Map

Status: initial implementation ledger.
Date: 2026-06-16

This ledger maps Monaco/VS Code editor roles to this readonly viewer. It is the
first stop for source-led future work: open the Monaco source, find the local
role, then check whether the local implementation is exact, partial,
readonly-subset, future, or skipped.

Allowed local statuses:

- `exact`: same role and substantially same data contract.
- `partial`: same role but missing behavior that matters to Monaco parity.
- `readonly-subset`: same role but intentionally smaller because the viewer is
  readonly.
- `future`: role is wanted but not implemented yet.
- `skipped`: role is intentionally out of scope for this plan.

| Monaco source | Monaco role | Local package/file/type | Local status | Readonly decision | UX impact if partial or skipped | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `vscode/src/vs/editor/common/core/range.ts` | `Range` | `core/document.mbt` / `Range` | readonly-subset | Keep in `core`; this viewer uses UTF-16 offset ranges rather than Monaco line/column ranges internally. | Partial line/column API means future selection and folding work must convert explicitly. | Package placement stays MoonBit-owned. |
| `vscode/src/vs/editor/common/core/position.ts` | `Position` | `core/document.mbt` / `Position` | readonly-subset | Keep in `core`; zero-based line/column positions are sufficient for current provider and coordinate conversion. | Future Monaco parity code must account for zero-based local positions versus Monaco's one-based positions. | Documented by `docs/architecture.md` UTF-16 convention. |
| `vscode/src/vs/editor/common/tokens/lineTokens.ts` | Per-line token data | `renderer/view_model/tokenized_document.mbt` / `TokenizedDocument` | partial | Store document snapshot plus tokens bucketed per model line; no mutable tokenization state stack in renderer. | Missing incremental line tokenization can increase recompute cost for very large edited documents, but the viewer is readonly and buckets once per document version. | Tokenizer registry lives in `renderer/browser`. |
| `vscode/src/vs/editor/common/viewModel.ts` | Common view model facade | `renderer/view_model/view_model.mbt` / `ViewModel` | readonly-subset | Default no-wrap behavior is preserved; option-controlled projected lines, folded hidden areas, and injected inlay-hint text exist for readonly viewing. | View zones remain pending. | Added as the common-layer spine for current viewport rendering. |
| `vscode/src/vs/editor/common/viewModel/viewModelImpl.ts` | View model implementation | `renderer/view_model/view_model.mbt` / `ViewModel` | readonly-subset | Plain struct and methods instead of Monaco service wiring and event emitters. | No editable model events or cursor-affecting view events. | Hosts still push provider results explicitly. |
| `vscode/src/vs/editor/common/viewModel/viewModelLines.ts` | View line collection | `renderer/view_model/view_model.mbt` / `ViewModelLines`, `ViewModelLinesFromModelAsIs`, `ViewModelLinesFromProjectedModel` | readonly-subset | The no-wrap fast path remains default; projected lines are selected for soft wrap, folded hidden areas, or injected inlay-hint text. | Selection/copy and view zones still need to consume these mappings. | Phase 7/8/9 projected role. |
| `vscode/src/vs/editor/common/viewModel/modelLineProjection.ts` | Projected view lines | `renderer/view_model/model_line_projection.mbt` / `ModelLineProjection` | partial | Readonly soft-wrap projection, folded hidden model lines, and injected-text projected columns exist. | Wrapped indentation, escaped-line-feed wrapping, and previous-break reuse remain pending. | Unit tests cover hidden-area and injected-text coordinates. |
| `vscode/src/vs/editor/common/modelLineProjectionData.ts` | Projection data | `renderer/view_model/model_line_projection_data.mbt` / `ModelLineProjectionData` | partial | Stores model line to projected view-line segments plus projected text metadata so hints can be rendered and mapped without corrupting model offsets. | Selection/copy serialization still needs to use the source/injected distinction. | Phase 7/8/9 common-layer slice. |
| `vscode/src/vs/editor/common/viewModel/monospaceLineBreaksComputer.ts` | Monospace wrap computer | `renderer/view_model/monospace_line_breaks_computer.mbt` / `LineBreaksComputer` | partial | Uses measured-column tab expansion, full-width code unit width, Monaco-style break-before/break-after/CJK classifier rules, and projected injected text before wrapping. | Wrapped indentation, escaped-line-feed wrapping, and previous-break reuse remain pending. | Phase 7/9 common-layer slice. |
| `vscode/src/vs/editor/common/coordinatesConverter.ts` | Model/view coordinate conversion | `renderer/view_model/view_model.mbt`, `view_model_lines_projected.mbt` / `CoordinatesConverter`, `IdentityCoordinatesConverter`, `ProjectedCoordinatesConverter` | partial | Identity conversion remains the default; projected conversion supports soft wrap, hidden folded lines, and injected-text columns. | Selection ranges still need Phase 10 copy/selection semantics. | Unit tests cover identity, projected conversion, wrapped hit testing, hidden-area conversion, and injected-text conversion; component tests cover wrapped hover/diagnostic/scroll/folding/hint paths. |
| `vscode/src/vs/editor/common/viewModel/inlineDecorations.ts` | Inline decoration data | `renderer/view_line_renderer/inline_decorations.mbt` / `InlineDecoration`, `InlineDecorationType` | partial | Source decorations are projected through render-line source spans and skip injected spans by default. | Selection-affecting decoration semantics are still limited. | Rendered through `RenderLineInput`. |
| `vscode/src/vs/editor/common/viewLayout/lineDecorations.ts` | Line decoration normalization | `renderer/view_line_renderer/line_decorations.mbt` / `LineDecoration`, `LineDecorationsNormalizer` | exact | Ported normalization behavior for current readonly line rendering. | None for current decoration rendering. | Covered by Monaco reference tests. |
| `vscode/src/vs/editor/common/viewLayout/viewLinesViewportData.ts` | Viewport data | `renderer/view_layout/view_lines_viewport_data.mbt` / `ViewportData`; `renderer/view_line_renderer/view_line_rendering_data.mbt` / `ViewLineRenderingData` | readonly-subset | Built from `ViewModel` plus `ViewLayout`; exposes one-based inclusive view line numbers, offsets, visible range, and decorations. | No whitespace/view-zone viewport data yet, so view zones do not displace layout. | Current path is window-bounded. |
| `vscode/src/vs/editor/common/viewLayout/viewLineRenderer.ts` | Pure line HTML renderer | `renderer/view_line_renderer/render_line_renderer.mbt` / `render_view_line`, `render_view_line2` | partial | DOM-free HTML and character mapping for readonly source and injected hint spans. | Missing selection and advanced font handling limits exact parity. | Covered by Monaco reference and browser component tests. |
| `vscode/src/vs/editor/common/viewLayout/linesLayout.ts` | Lines layout | `renderer/view_layout/lines_layout.mbt` / `LinesLayout` | readonly-subset | Uniform-height line layout; view-zone slots exist but do not displace lines yet. | View zones cannot host peek-like panels until displacement lands. | Phase 11 completes view zones. |
| `vscode/src/vs/editor/common/viewLayout/viewLayout.ts` | View layout and scroll model | `renderer/view_layout/view_layout.mbt` / `ViewLayout`; `renderer/view_layout/scrollable.mbt` / `Scrollable`; `renderer/view_layout/scrollbar_state.mbt` / `ScrollbarState` | readonly-subset | Owns pure scroll truth, scrollbar geometry, and viewport derivation from the active view-line count. | View-zone displacement is still pending. | Browser applies DOM effects after wrap/fold changes rebuild the view model. |
| `vscode/src/vs/editor/browser/view.ts` | Browser view root | `renderer/browser/view.mbt` / `Island`, `Viewer::flush_render` | partial | Imperative readonly view rather than Monaco service-driven `View`. | Remaining coupling to frame notifications and root scheduling makes this a partial browser role. | File extraction is complete for the current readonly path. |
| `vscode/src/vs/editor/browser/view/viewLayer.ts` | View layer recycler | `renderer/browser/view_layer.mbt` / `Island::render_lines` | partial | Recycles visible line and gutter nodes and limits HTML rewrites. | Still uses `RenderFrame` for line node classes and gutter metadata during the compatibility period. | Line HTML input comes from `ViewModel` -> `ViewportData`. |
| `vscode/src/vs/editor/browser/viewParts/viewLines/viewLine.ts` | Per-line DOM application | `renderer/browser/view_line.mbt` / `write_line_node` | partial | Builds `RenderLineInput` and writes line HTML for visible lines. | Direction attributes and projected-line data are incomplete. | Future projection phases should expand this role. |
| `vscode/src/vs/editor/browser/viewParts/contentWidgets/contentWidgets.ts` | Content widgets | `renderer/browser/content_widgets.mbt` / content widget slots and hover widget | partial | Hover is the first content widget. | Generic content-widget lifecycle is minimal; future hint/peek-like widgets need more structure. | Text-anchored slot and overflowing slot are explicit. |
| `vscode/src/vs/editor/browser/viewParts/overlayWidgets/overlayWidgets.ts` | Overlay widgets | `renderer/browser/overlay_widgets.mbt` / overlay widget slots | readonly-subset | Stable DOM slots exist; no concrete overlay widgets yet. | Low for current readonly hover and scroll behavior. | Slot remains for future controls. |
| `vscode/src/vs/editor/browser/viewParts/viewZones/viewZones.ts` | View zones | `renderer/browser/view_zones.mbt` / `.view-zones`, `renderer/view_layout/view_zone.mbt` / `ViewZone` | future | DOM slot and hit-test data exist; real line displacement is future. | High for peek-like panels and inline readonly UI. | Phase 11. |
| `vscode/src/vs/editor/browser/viewParts/viewOverlays/viewOverlays.ts` | View overlays | `renderer/browser/view_overlays.mbt` / `.view-overlays` | readonly-subset | Stable slot exists; diagnostics currently use line decorations rather than overlay parts. | Medium for selection/current-line parity. | Selection phase should revisit. |
| `vscode/src/vs/editor/browser/viewParts/margin/margin.ts` | Margin and line numbers | `renderer/browser/margin.mbt`, `view_layer.mbt` / margin, line-number nodes, folding markers | partial | Renders line numbers plus readonly fold/unfold markers for foldable ranges. | Nested marker behavior and accessibility polish remain partial. | Phase 8 component coverage exercises fold/unfold. |
| `vscode/src/vs/base/browser/ui/scrollbar/scrollableElement.ts` | Scrollable element and custom scrollbars | `renderer/browser/scrollable_element.mbt` / `ScrollableElementDom` | partial | Shared editor/hover DOM-backed scrollable element with Monaco-shaped custom scrollbar behavior. | Some Monaco option surface and edge-case classes remain unimplemented. | Covered by conformance tests. |
| `vscode/src/vs/editor/contrib/hover/browser/contentHoverWidget.ts` | Content hover widget | `renderer/browser/hover_controller.mbt`, `hover_render.mbt`, `content_widgets.mbt` | partial | Readonly hover behavior and DOM structure only. | Remaining exact placement and advanced hover participant behavior can still diverge. | Prior conformance work covers core shell and scrollbar. |
| `vscode/src/vs/editor/contrib/folding/browser/foldingModel.ts` | Folding model | `renderer/view_model/folding_model.mbt`, `renderer/browser/folding.mbt` / `FoldingModel`, browser folded-state controller | partial | Normalizes folded ranges, derives merged hidden-line spans, and toggles readonly folded state per current document. | Persistence across document revisions and richer nested behavior remain partial. | Phase 8. |
| `vscode/src/vs/editor/contrib/folding/browser/hiddenRangeModel.ts` | Hidden ranges | `renderer/view_model/folding_model.mbt` / `HiddenRange` | partial | Represents hidden model-line spans with the folding header line still visible and projects them out of view lines. | Selection still needs to consume hidden ranges in Phase 10. | Phase 8. |
| `vscode/src/vs/editor/contrib/folding/browser/foldingRanges.ts` | Folding ranges | `language/folding.mbt`; `renderer/browser/services.mbt`; `remote_protocol/protocol.mbt` / `FoldingRange`, `FoldingRangeKind`, `FoldingRangeProvider` | partial | Provider-facing readonly folding contracts exist with browser collection, MoonBit indentation fallback, and optional remote transport. | Native LSP-specific folding support is not implemented; fallback covers MoonBit when providers return empty. | Phase 8. |
| `vscode/src/vs/editor/contrib/inlayHints/browser/inlayHints.ts` | Inlay hint data/rendering | `language/inlay_hints.mbt`; `renderer/view_model/injected_text.mbt`; `renderer/browser/services.mbt` | partial | Provider contracts, optional remote transport, projected injected-text rendering, styling, and tooltip hover exist for readonly hints. | Refresh/caching policy is simple provider collection; selection/copy exclusion lands in Phase 10. | Phase 9. |
| `vscode/src/vs/editor/contrib/inlayHints/browser/inlayHintsController.ts` | Inlay hint controller | `renderer/browser/registry.mbt`, `hover_participants.mbt` | partial | Browser requests hint providers per document version, stores current hints, rebuilds the view model, and contributes hint tooltip hover synchronously. | No separate hint cache or advanced enablement options yet. | Phase 9 component coverage. |
| `vscode/src/vs/editor/browser/controller/editContext/clipboardUtils.ts` | Clipboard serialization helpers | Future `renderer` role | future | Wanted for readonly selection/copy only. | High: users cannot select/copy with Monaco parity. | Phase 10. |
| `vscode/src/vs/editor/contrib/clipboard/browser/clipboard.ts` | Browser clipboard controller | Future `renderer/browser` role | future | Copy bridge only; no editable paste path. | High for source interaction. | Phase 10. |
| `vscode/src/vs/editor/browser/viewParts/selections/selections.ts` | Selection overlay | Future `renderer/browser` role | future | Readonly selection rendering only. | High: no Monaco-quality drag selection/copy yet. | Phase 10. |
| `vscode/src/vs/editor/browser/controller/editContext/*` | Editable cursor, textarea, IME, edit context | No local role | skipped | Editing is out of scope for the readonly viewer. | High for a full editor, intentionally none for readonly display. | Requires separate editable-editor plan. |
| `vscode/src/vs/editor/browser/viewParts/minimap/*` | Minimap | No local role | skipped | Out of scope for this plan. | Medium for large-file navigation, but not required for current readonly parity. | Could be a separate feature plan. |
| `vscode/src/vs/editor/browser/viewParts/overviewRuler/*` | Overview ruler | No local role | skipped | Out of scope for this plan. | Medium for diagnostics overview and navigation. | Could be a separate feature plan. |
| `vscode/src/vs/editor/browser/widget/codeEditor/codeEditorWidget.ts` and accessibility sources | Accessibility model | No local role | skipped | This plan does not implement Monaco's accessibility textarea, screen-reader model, or ARIA-rich interaction model. | High: visual/source interaction parity is not accessibility parity. | Requires a dedicated follow-up plan. |
